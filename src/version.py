# version.py
# contains version information

# global todos go here:
# TODO: figure out what relay 2 does

__version_info = (0, 1, 0)
__version__ = '.'.join(map(str, __version_info))

version = __version__
prog_name = 'Pi_control'
